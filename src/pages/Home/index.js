import React from "react";
import { View, Text, TouchableOpacity } from "react-native";

import { Container } from "./styles";

const Home = ({ navigation }) => {
  return <View justifyContent="center" alignItems="center" flex={1}></View>;
};

export default Home;
